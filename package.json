{
  "name": "whatsapp-translation-server",
  "version": "1.0.0",
  "private": true,
  "description": "WhatsApp Translation Server with Italian-English translation and word-of-hour feature",
  "workspaces": [
    "backend",
    "shared"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=backend",
    "build": "npm run build --workspace=shared && npm run build --workspace=backend",
    "start": "npm run start --workspace=backend",
    "typecheck": "npm run typecheck --workspaces"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}
